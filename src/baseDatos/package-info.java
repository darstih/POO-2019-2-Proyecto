/**
 * Capa de persistencia: En esta el sistema deberá guardar en archivo(s) de texto (.txt) el estado de todos los objetos del
sistema en un momento dado. Así mismo el sistema permitirá cargar el estado guardado cargando todos los objetos
en memoria ram para continuar trabajando el sistema. Para tal fin se implementará las clases necesarias que permitan
esto y se ubicarán en un paquete que llamaremos BaseDatos. Los archivos utilizados para implementar la persistencia
del sistema se guardarán en un directorio relativo del proyecto llamado /temp que estará al interior de este paquete.
 *
 *Creo que este debe ser el que maneje JSON
 */
/**
 * @author andor
 *
 */
package baseDatos;